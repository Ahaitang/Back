<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hospital.neuroimmune.mapper.MedicationMapper">

    <resultMap id="BaseResultMap" type="org.hospital.neuroimmune.entity.Medication">
        <id column="id" property="id"/>
        <result column="patient_id" property="patientId"/>
        <result column="patient_name" property="patientName"/>
        <result column="doctor_id" property="doctorId"/>
        <result column="doctor_name" property="doctorName"/>
        <result column="medication_name" property="medicationName"/>
        <result column="date" property="date"/>
        <result column="dosage" property="dosage"/>
        <result column="unit" property="unit"/>
        <result column="frequency" property="frequency"/>
        <result column="route" property="route"/>
        <result column="duration" property="duration"/>
        <result column="notes" property="notes"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, patient_id, patient_name, doctor_id, doctor_name, medication_name, date,
        dosage, unit, frequency, route, duration, notes, create_time
    </sql>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medication
        <where>
            <if test="patientId != null">
                AND patient_id = #{patientId}
            </if>
            <if test="doctorId != null">
                AND doctor_id = #{doctorId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (patient_name LIKE CONCAT('%', #{keyword}, '%')
                     OR medication_name LIKE CONCAT('%', #{keyword}, '%')
                     OR doctor_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="startDate != null and startDate != ''">
                AND date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY date DESC, create_time DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM medication
        <where>
            <if test="patientId != null">
                AND patient_id = #{patientId}
            </if>
            <if test="doctorId != null">
                AND doctor_id = #{doctorId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (patient_name LIKE CONCAT('%', #{keyword}, '%')
                     OR medication_name LIKE CONCAT('%', #{keyword}, '%')
                     OR doctor_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="startDate != null and startDate != ''">
                AND date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND date &lt;= #{endDate}
            </if>
        </where>
    </select>

    <select id="selectCountByDoctorId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM medication WHERE doctor_id = #{doctorId}
    </select>

    <select id="selectListByDoctorId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medication
        WHERE doctor_id = #{doctorId}
        <if test="request.keyword != null and request.keyword != ''">
            AND (patient_name LIKE CONCAT('%', #{request.keyword}, '%')
                 OR medication_name LIKE CONCAT('%', #{request.keyword}, '%'))
        </if>
        <if test="request.startDate != null and request.startDate != ''">
            AND date &gt;= #{request.startDate}
        </if>
        <if test="request.endDate != null and request.endDate != ''">
            AND date &lt;= #{request.endDate}
        </if>
        ORDER BY date DESC, create_time DESC
        LIMIT #{request.pageSize} OFFSET #{request.offset}
    </select>

</mapper>