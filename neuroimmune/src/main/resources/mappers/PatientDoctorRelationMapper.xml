<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hospital.neuroimmune.mapper.PatientDoctorRelationMapper">

    <resultMap id="RelationResultMap" type="org.hospital.neuroimmune.entity.PatientDoctorRelation">
        <id property="id" column="id"/>
        <result property="patientId" column="patient_id"/>
        <result property="patientName" column="patient_name"/>
        <result property="doctorId" column="doctor_id"/>
        <result property="doctorName" column="doctor_name"/>
        <result property="relationType" column="relation_type"/>
        <result property="status" column="status"/>
        <result property="bindMethod" column="bind_method"/>
        <result property="remark" column="remark"/>
        <result property="bindTime" column="bind_time"/>
        <result property="unbindTime" column="unbind_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, patient_id, patient_name, doctor_id, doctor_name, relation_type, status,
        bind_method, remark, bind_time, unbind_time, create_time
    </sql>

    <select id="selectByPatientId" resultMap="RelationResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM patient_doctor_relation
        WHERE patient_id = #{patientId}
        ORDER BY bind_time DESC
    </select>

    <select id="selectActiveByPatientId" resultMap="RelationResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM patient_doctor_relation
        WHERE patient_id = #{patientId} AND status = 'active'
        ORDER BY bind_time DESC
        LIMIT 1
    </select>

    <select id="selectByDoctorId" resultMap="RelationResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM patient_doctor_relation
        WHERE doctor_id = #{doctorId}
        ORDER BY bind_time DESC
    </select>

    <select id="selectActiveByDoctorId" resultMap="RelationResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM patient_doctor_relation
        WHERE doctor_id = #{doctorId} AND status = 'active'
        ORDER BY bind_time DESC
    </select>

    <select id="selectByPatientAndDoctor" resultMap="RelationResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM patient_doctor_relation
        WHERE patient_id = #{patientId} AND doctor_id = #{doctorId}
        LIMIT 1
    </select>

    <select id="selectList" resultMap="RelationResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM patient_doctor_relation
        <where>
            <if test="patientName != null and patientName != ''">
                AND patient_name LIKE CONCAT('%', #{patientName}, '%')
            </if>
            <if test="doctorName != null and doctorName != ''">
                AND doctor_name LIKE CONCAT('%', #{doctorName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY bind_time DESC
    </select>

    <select id="countByDoctorId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM patient_doctor_relation
        WHERE doctor_id = #{doctorId} AND status = 'active'
    </select>

    <update id="unbind">
        UPDATE patient_doctor_relation
        SET status = 'inactive', unbind_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>