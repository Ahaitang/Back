<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hospital.qmg.mapper.QuestionnaireOptionMapper">

    <resultMap id="optionResultMap" type="org.hospital.qmg.entity.QuestionnaireOption">
        <id property="id" column="id"/>
        <result property="itemKey" column="item_key"/>
        <result property="label" column="label"/>
        <result property="value" column="value"/>
        <result property="score" column="score"/>
        <result property="displayOrder" column="display_order"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByItemKey" parameterType="String" resultMap="optionResultMap">
        SELECT id, item_key, label, value, score, display_order, create_time, update_time
        FROM questionnaire_option
        WHERE item_key = #{itemKey}
        ORDER BY display_order ASC, id ASC
    </select>

    <select id="findById" parameterType="Integer" resultMap="optionResultMap">
        SELECT id, item_key, label, value, score, display_order, create_time, update_time
        FROM questionnaire_option
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="org.hospital.qmg.entity.QuestionnaireOption" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO questionnaire_option (item_key, label, value, score, display_order, create_time, update_time)
        VALUES (#{itemKey}, #{label}, #{value}, #{score}, #{displayOrder}, #{createTime}, #{updateTime})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO questionnaire_option (item_key, label, value, score, display_order, create_time, update_time)
        VALUES
        <foreach collection="options" item="option" separator=",">
            (#{option.itemKey}, #{option.label}, #{option.value}, #{option.score}, #{option.displayOrder}, #{option.createTime}, #{option.updateTime})
        </foreach>
    </insert>

    <update id="update" parameterType="org.hospital.qmg.entity.QuestionnaireOption">
        UPDATE questionnaire_option
        SET label = #{label},
            value = #{value},
            score = #{score},
            display_order = #{displayOrder},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Integer">
        DELETE FROM questionnaire_option WHERE id = #{id}
    </delete>

    <delete id="deleteByItemKey" parameterType="String">
        DELETE FROM questionnaire_option WHERE item_key = #{itemKey}
    </delete>

</mapper>
