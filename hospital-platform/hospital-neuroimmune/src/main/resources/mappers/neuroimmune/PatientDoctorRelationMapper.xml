<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hospital.neuroimmune.mapper.PatientDoctorRelationMapper">

    <!-- 动态 SQL 查询：条件筛选关系列表 -->
    <select id="selectRelationList" resultType="org.hospital.neuroimmune.entity.PatientDoctorRelation">
        SELECT id, patient_id, patient_name, doctor_id, doctor_name, relation_type, status,
               bind_method, remark, bind_time, unbind_time, create_time
        FROM patient_doctor_relation
        <where>
            <if test="patientName != null and patientName != ''">
                AND patient_name LIKE CONCAT('%', #{patientName}, '%')
            </if>
            <if test="doctorName != null and doctorName != ''">
                AND doctor_name LIKE CONCAT('%', #{doctorName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY bind_time DESC
    </select>

</mapper>